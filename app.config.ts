export default defineAppConfig({
  ui: {
    colorMode: {
      preference: "light",
    },
    container: {
      constrained: "max-w-7xl",
    },
    button: {
      default: {
        size: "md",
        color: "gray",
        variant: "ghost",
      },
    },
  },
});
