export default defineAppConfig({
  ui: {
    colorMode: {
      preference: "light",
    },
    button: {
      default: {
        size: "md",
        color: "gray",
        variant: "ghost",
      },
    },
  },
});
